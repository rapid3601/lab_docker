# 🐳 Lab-Docker: Контейнеризированное Веб-Приложение (FastAPI + PostgreSQL)

## Архитектура
Проект состоит из двух контейнеризированных сервисов, запущенных через Docker Compose:
1.  **`app` (Бэкенд):** Приложение на Python/FastAPI. Предоставляет эндпоинт `/health`, который проверяет доступность самого сервиса и его способность подключиться к БД.
2.  **`db` (База данных):** Сервис на базе образа PostgreSQL (postgres:15-alpine), использующий именованный том для сохранения данных.

Сервисы изолированы в общей сети Docker Compose, где бэкенд обращается к базе данных по имени хоста `db`.

## 🚀 Инструкция по Запуску

**Необходимые условия:** Установленный Docker и Docker Compose.

1.  **Клонировать репозиторий:**
    ```bash
    git clone [ваш_репозиторий]
    cd lab-docker
    ```
2.  **Собрать и запустить сервисы:**
    ```bash
    docker-compose up --build -d
    ```
3.  **Проверить работоспособность:**
    ```bash
    curl http://localhost:8000/health
    ```
    Ожидаемый ответ: `{"status": "OK", "db_status": "OK"}`

## 💡 Обоснование Архитектурных Решений

### 1. Базовый образ
* **Выбран:** `python:3.11-slim`.
* **Обоснование:** Использование `slim` образа значительно уменьшает конечный размер контейнера по сравнению с полным образом (`latest` или обычный `3.11`). Это ускоряет развертывание, уменьшает потребление места и, что самое главное, уменьшает **поверхность атаки**, так как содержит меньше ненужных пакетов.

### 2. Безопасность (Non-root Пользователь)
* **Решение:** В Dockerfile создан специальный пользователь `appuser` (не-root), и все последующие команды запускаются от его имени (`USER appuser`).
* **Обоснование:** Запуск процессов от имени пользователя `root` в контейнере — это риск безопасности. Если злоумышленник скомпрометирует приложение, он получит root-доступ внутри контейнера, что может быть использовано для дальнейшего проникновения в хост-систему. Non-root пользователь минимизирует этот риск.

### 3. Порядок в Dockerfile (Кэширование)
Порядок команд критически важен для эффективного кэширования слоев Docker:
1.  **Установка зависимостей (`COPY requirements.txt` и `RUN pip install`)** происходит до копирования основного кода приложения.
2.  **Обоснование:** Файл `requirements.txt` меняется гораздо реже, чем код приложения. Если код приложения изменяется, Docker может использовать кэшированный слой установки зависимостей, **пропуская** длительную команду `RUN pip install` и начиная сборку с момента копирования кода. Это значительно ускоряет итеративную разработку и сборку.

## 📸 Скриншоты Проверок

#### 1. `docker-compose ps`
```bash
# [Вставьте скриншот/текст вывода команды docker-compose ps]
