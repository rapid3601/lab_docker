# Web Application Containerization (Lab-Docker)

Данный проект представляет собой пример контейнеризированного веб-приложения, состоящего из бэкенда на **FastAPI (Python)** и базы данных **PostgreSQL**. Проект демонстрирует практики работы с Docker: многослойную сборку, безопасность и оркестрацию сервисов.



## 🏗 Архитектура

Проект построен на микросервисной архитектуре:
1.  **Backend (App):** Python-приложение на базе FastAPI с endpoint'ом `/health`.
2.  **Database (DB):** Официальный образ PostgreSQL 15-alpine.
3.  **Network:** Изолированная сеть Docker (bridge), создаваемая автоматически.
4.  **Storage:** Именованный том (Volume) `postgres_data` для сохранения данных базы при перезапуске.

---

## 🚀 Инструкция по запуску

Для запуска приложения вам понадобятся установленные **Docker** и **Docker Compose**.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/rapid3601/lab-docker.git
    cd lab-docker
    ```

2.  **Запустите проект в фоновом режиме:**
    ```bash
    docker-compose up --build -d
    ```

3.  **Проверьте работоспособность:**
    ```bash
    curl http://localhost:8000/health
    ```

---

## 🛠 Обоснование технических решений

### 1. Базовый образ
Использован образ `python:3.11-slim`.
* **Обоснование:** Версия `slim` обеспечивает баланс между малым размером образа и наличием необходимых системных зависимостей. Фиксация версии `3.11` гарантирует воспроизводимость сборки на разных машинах.

### 2. Безопасность (Non-root user)
В `Dockerfile` создается пользователь без прав суперпользователя:
```dockerfile
RUN useradd -m appuser
USER appuser
