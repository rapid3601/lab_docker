\# 🐳 Lab-Docker: Контейнеризированное Веб-Приложение (FastAPI + PostgreSQL)



\## Архитектура

Проект состоит из двух контейнеризированных сервисов:

1\.  \*\*`app` (Бэкенд):\*\* Приложение на Python/FastAPI. Предоставляет эндпоинт `/health`, который проверяет доступность самого сервиса и его способность подключиться к БД.

2\.  \*\*`db` (База данных):\*\* Сервис на базе образа PostgreSQL (postgres:15-alpine), использующий именованный том для сохранения данных.



Сервисы запущены через Docker Compose и находятся в общей сети.



---



\## 🚀 Инструкция по Запуску



\*\*Необходимые условия:\*\* Установленный Docker и Docker Compose.



1\.  \*\*Клонировать репозиторий (или создать папку):\*\*

&nbsp;   ```bash

&nbsp;   git clone \[ваш\_репозиторий]

&nbsp;   cd lab-docker

&nbsp;   ```

2\.  \*\*Собрать и запустить сервисы:\*\*

&nbsp;   ```bash

&nbsp;   docker-compose up --build -d

&nbsp;   ```

3\.  \*\*Проверить работоспособность:\*\*

&nbsp;   ```bash

&nbsp;   curl http://localhost:8000/health

&nbsp;   ```

&nbsp;   Ожидаемый ответ: `{"status": "OK", "db\_status": "OK"}`



---



\## 💡 Обоснование Архитектурных Решений



\### 1. Выбранный Базовый Образ: `python:3.11-slim`

\* \*\*Обоснование:\*\* Использование `slim` образа (вместо полного) значительно уменьшает конечный размер контейнера. Это ускоряет загрузку и развертывание, а также уменьшает \*\*поверхность атаки\*\* (меньше лишних пакетов), что критично для безопасности.



\### 2. Обеспечение Безопасности (Non-root Пользователь)

\* \*\*Решение:\*\* В Dockerfile созданы пользователь `appuser` и группа `appgroup`. Приложение запускается командой `CMD` через `gosu appuser ...`.

\* \*\*Обоснование:\*\* Запуск процессов от имени `root` в контейнере является риском. `gosu` позволяет выполнить команду запуска (которая требует прав `root` для настройки среды) от имени `root`, но сам процесс `uvicorn` запускается как `appuser`, минимизируя потенциальный ущерб в случае компрометации.



\### 3. Порядок Команд в Dockerfile

Порядок команд (`COPY requirements.txt` до `COPY . .`) выбран для оптимизации \*\*кэширования слоев Docker\*\*.

\* \*\*Обоснование:\*\* Команда `RUN pip install` (установка зависимостей) занимает много времени. Поскольку файл `requirements.txt` меняется реже, чем код (`.`), Docker кэширует слой установки зависимостей. При изменении только кода Docker использует кэш и начинает сборку сразу с копирования кода, что \*\*резко ускоряет итеративную разработку\*\*.



---



\## 📸 Скриншоты Проверок



\#### 1. Статус контейнеров (`docker-compose ps`)



\[Вставьте скриншот вывода команды `docker-compose ps` здесь]



\#### 2. Проверка здоровья (`curl /health`)



\[Вставьте скриншот вывода команды `curl http://localhost:8000/health` с ответом JSON здесь]



---



\*\*Последний шаг:\*\* После того как вы сделали все скриншоты, выполните команду для остановки и удаления контейнеров и томов, чтобы завершить задание:



```powershell

docker-compose down -v

